#include <stdio.h>
/**
 * main - a program that prints all possible combinations of single-digit numbers
 *
 * Reasulte: resulte is 0 (success)
 */
int main(void)
{
	int a, b, c;
	for (a = 1; a < 3; a++)
		putchar(a);
		for (b = 1; b < 3; b++)
			putchar(b);
			for (c = 1; b < 3; c++)
				putchar(c);
	putchar('\n')
	return (0);
}
 				

