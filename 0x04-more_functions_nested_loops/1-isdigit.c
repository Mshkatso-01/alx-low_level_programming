#include <stdio.h>
#include "main.h"
/**
 * _isdigit - a function that checks for a digit (0 through 9)
 * @c: parameter has an int type
 * Return: return 1 or 0
 */
int _isdigit(int c)
{
	c = '5';
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
