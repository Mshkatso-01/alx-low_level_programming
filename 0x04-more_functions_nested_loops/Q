#include <stdio.h>
#include "main.h"
/**
 * main - a function that checks for uppercase character.
 * Return - Always return 0 (success)
 */
int _isupper(int c);
{
	for (c = '65'; c <= '90'; c++)
	{
		return (1);
	}
	else
		return (0);
